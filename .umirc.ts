import { defineConfig } from 'dumi';

let BaseUrl = ''
export default defineConfig({
  title: 'kely UI',
  favicon: '/images/myui.png',
  logo: '/images/myui.png', 
  outputPath: 'docs-dist',
  // more config: https://d.umijs.org/config
});
 